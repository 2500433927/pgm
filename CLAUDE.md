# CLAUDE.md

本文件是项目的全局工作指令，适用于仓库内所有任务（frontend/、backend/、admin-web/ 等）。

## 主要事项
1. 代码注释尽量用中文
2. 每次修改完代码需要你返回一个版本号，方便我后续进行回滚
3. 每次修改完代码后，在项目根目录的 `pro_chang.md` 中追加该版本号的变更记录（版本号 + 变更内容摘要），保持变更日志最新,同时在当前项目的根目录下创建一个pro_chang.md文档来记录每次版本的变更内容

## 项目知识库（repowiki）读取规则

- **每新开一个会话时，先阅读 `repowiki/` 目录下的部分内容**，以项目 wiki 为准执行任务，不凭猜测或重新推导已文档化的结论。
- `repowiki/` 是全中文（zh-CN）的项目知识库，覆盖 API 接口文档、前端架构、后端架构、多语言国际化、开发指南、支付系统、数据统计系统、比赛系统、部署运维等主题（见 `repowiki/wiki_plan.yaml`）。

### 阅读方式（提高效率）

1. 优先看 - `repowiki\zh\content\快速开始.md`—— 适合快速开发 
   再浏览 `repowiki/` 目录结构，定位与当前任务相关的分类：
   - `repowiki/zh/content/` —— 按主题分类的项目文档（开发指南、前后端架构、API 接口、部署运维等）
   - `repowiki/knowledge/zh/` —— 按主题归档的技术知识文章
2. 通读与当前任务相关的所有页面；任务涉及不熟悉的领域时，先读对应分类下的全部页面再动手。
3. wiki 内容与代码不一致时，以代码实际状态为准，并在交付说明中注明差异。



